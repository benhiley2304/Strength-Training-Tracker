{
  "name": "strength-training-tracker",
  "version": "5.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check app.js && node --check model.js && node --check programme.js && node --check sw.js"
  }
}
